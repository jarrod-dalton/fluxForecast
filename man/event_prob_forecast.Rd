% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/streaming_summaries.R
\name{event_prob_forecast}
\alias{event_prob_forecast}
\title{Memory-light event risk summaries without storing all simulated trajectories}
\usage{
event_prob_forecast(
  engine,
  entities,
  times,
  event,
  by = c("run", "entity", "entity_param_draw"),
  S = 200,
  param_sets = NULL,
  start_time = NULL,
  terminal_events = NULL,
  condition_on_events = NULL,
  eligible = NULL,
  ctx = NULL,
  max_events = 1000,
  seed = NULL,
  backend = c("none", "mclapply", "future"),
  n_workers = NULL
)
}
\arguments{
\item{engine}{A fluxCore Engine.}

\item{entities}{A list of Entity objects (or a single Entity).}

\item{times}{Numeric vector of forecast times.}

\item{event}{Character vector of event types of interest.}

\item{by}{Grouping level for summaries. Default is \code{"run"}.}

\item{S}{Simulations per (entity, param_set).}

\item{param_sets}{Optional list of parameter lists.}

\item{start_time}{Scalar time in \code{times} defining the fixed eligible cohort.}

\item{terminal_events}{Optional event types that must be absent by \code{start_time}.}

\item{condition_on_events}{Optional event types that must be absent by
\code{start_time}.}

\item{eligible}{Optional function(snapshot, time, ctx) -> TRUE/FALSE,
evaluated at \code{start_time}.}

\item{ctx}{Optional list passed to \code{eligible()}.}

\item{max_events}{Maximum events per run.}

\item{seed}{Optional base seed.}

\item{backend}{Parallel backend. One of \code{c("none", "mclapply", "future")}.}

\item{n_workers}{Optional number of workers for \code{mclapply} or an ephemeral
\code{future::multisession} plan.}
}
\value{
A \code{flux_event_prob} object.
}
\description{
Runs forward simulations and computes event risk curves as the simulations run.
This returns a compact \code{flux_event_prob} object and avoids keeping a large
\code{flux_forecast} (which can be memory-heavy when \code{S} or \code{times} is large).
}
