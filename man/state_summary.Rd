% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state_summary.R
\name{state_summary}
\alias{state_summary}
\title{Summarize forecasted state variables over time}
\usage{
state_summary(
  x,
  vars = NULL,
  times = NULL,
  categorical_max_levels = 50,
  by = c("run", "entity", "entity_param_draw")
)
}
\arguments{
\item{x}{A \code{flux_forecast}.}

\item{vars}{Character vector of state/derived variable names. Defaults to all
variables stored in the forecast object.}

\item{times}{Numeric vector of times at which to summarize. Defaults to
\code{x$times}.}

\item{categorical_max_levels}{Maximum number of categorical levels allowed for
a single variable.}

\item{by}{Grouping level for summaries:
\code{"run"} pools all eligible runs (default), \code{"entity"} summarizes within
each \code{entity_id}, and \code{"entity_param_draw"} summarizes within each
\verb{(entity_id, param_draw_id)}.}
}
\value{
A named list with one element per variable. Each element is a
\code{data.frame}.
}
\description{
Summarize the distribution of state variables at requested times among runs
that are alive and defined at each time point.
}
\details{
Eligibility is evaluated at each time point using the forecast object's
\code{alive} matrix: \code{!is.na(alive) & alive == TRUE}.
}
